# requirements.txt – dependencias unificadas
# pip install -r requirements.txt

# ── FastAPI core ────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Plantillas Jinja2 (App A) ───────────────────────────────────
jinja2>=3.1.0
python-multipart>=0.0.9

# ── Configuración unificada (Pydantic v2) ───────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ── MongoDB (App B) ─────────────────────────────────────────────
pymongo>=4.6.0

# ── HTTP client (App B – Meta Graph API) ────────────────────────
requests>=2.32.0

# ── Scheduler (App A – trick_runner) ────────────────────────────
apscheduler>=3.11.0

# ── Session middleware (App A – SessionMiddleware) ───────────────
itsdangerous>=2.2.0
